Switch back to the main branch and pull the latest changes after a PR merge.

1. Run `git checkout main`
2. Run `git pull origin main`
3. Delete the merged feature branch locally with `git branch -d <branch-name>` (use the branch you were just on)
4. Confirm the current branch and latest commit
