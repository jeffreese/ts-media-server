/**
 * Permission component identifiers (must match stored role/component keys) and
 * the numeric threshold for admin-level access.
 */

/** SysAdmin permission component. */
export const SYSADMIN_KEY = 'SysAdmin';
/** UserAdmin permission component. */
export const USERADMIN_KEY = 'UserAdmin';
/** Media permission component. */
export const MEDIA_KEY = 'Media';
/** Contact permission component. */
export const CONTACT_KEY = 'Contact';

/** Access levels at or above this value are treated as admin. */
export const ADMIN_ACCESS_LEVEL = 5;
